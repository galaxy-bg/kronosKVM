# Virtual Media

Virtual media is inactive. Future images live under
`/var/lib/kronoskvm/images`, are validated before attachment and default to
read-only. Upload size, safe detach and gadget recovery policies are required.
