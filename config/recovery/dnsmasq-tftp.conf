# Independent read-only TFTP process; does not provide DNS or DHCP.
port=0
interface=br-recovery
listen-address=192.168.34.100
bind-dynamic
enable-tftp=br-recovery
tftp-root=/mnt/kronoskvm-storage/recovery
tftp-secure
log-facility=-
